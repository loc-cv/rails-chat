<div class="flex flex-col gap-1 self-start message-box-<%= message.user.id %> max-w-[40%]">
  <div class="flex gap-2 items-end">
    <div class="avatar-<%= message.user.id %> min-w-[25px]">
      <%= image_tag message.user.chat_avatar, class: "rounded-full" %>
    </div>

    <div class="break-words bg-white p-4 rounded-xl message-<%= message.user.id %>"}>
      <% unless message.room.is_private? %>
        <p class="text-sm font-semibold text-gray-600 mb-2">
          <%= message.user.name %>
        </p>
      <% end %>
      <p class="text-gray-800 font-medium">
        <%= message.body %>
      </p>
    </div>
  </div>

  <p class="text-xs text-gray-400 self-end time-<%= message.user.id %>">
    <%= time_ago_in_words(message.created_at).capitalize %>
  </p>
</div>
